[package]
name = "savvy_benchmarks"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]

[dependencies]
savvy = "*"
savvy-ffi = "*"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true